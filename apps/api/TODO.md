# Remaining tasks

- [ ] API-001: Evaluate KV payload compression for cached event/report data once production usage shows pressure (for example: approaching free-tier storage limits, hitting value-size constraints, or clear cache cost/latency regressions).
