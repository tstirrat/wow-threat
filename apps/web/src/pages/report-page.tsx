/**
 * Report landing route content under the shared report layout.
 */
import type { FC } from 'react'

/** Render no extra content below the shared report layout chrome. */
export const ReportPage: FC = () => null
